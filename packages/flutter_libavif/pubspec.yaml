name: flutter_libavif
description: Flutter ImageProvider adapters for source-built native libavif decoding.
version: 0.1.0
repository: https://github.com/dartgfx/libavif
topics:
  - avif
  - flutter
  - image

environment:
  sdk: ^3.12.0
  flutter: '>=3.41.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  libavif: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
